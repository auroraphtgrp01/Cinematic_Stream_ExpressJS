export enum UserVerifyStatus {
  Unverified,
  Verified,
  Blocked
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum VideoUploadType {
  HLS,
  Video
}
